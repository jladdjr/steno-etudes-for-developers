# Etude 8: Grammar

```
The variable stores a value.
The function is defined here.
Evaluate the expression at this point.

Access is either private or public.

It is not in scope.
It belongs to the namespace.

The object is passed in as an argument.
There are three named parameters.

The method signature changed.

You can convert it from a string to an integer.
A float is used since it is a decimal.
The object is the wrong type.

Four libraries are imported.

You can check the syntax with a linter.
```

